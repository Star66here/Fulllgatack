document.addEventListener('DOMContentLoaded', () => {
    document.querySelectorAll('.btc-custom').forEach(button => {
        button.addEventListener('click', (event) => {
            
        })
    })
})